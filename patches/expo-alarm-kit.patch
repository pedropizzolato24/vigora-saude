diff --git a/ios/ExpoAlarmKit.podspec b/ios/ExpoAlarmKit.podspec
index 83351543845b08852d1c2042a5597a1260bb4e7c..51a176f193ad205c9bce70c73e86733e3cf2f9d8 100644
--- a/ios/ExpoAlarmKit.podspec
+++ b/ios/ExpoAlarmKit.podspec
@@ -11,7 +11,7 @@ Pod::Spec.new do |s|
   s.author         = package['author']
   s.homepage       = package['homepage']
   s.platforms      = {
-    :ios => '26.1',
+    :ios => '15.1',
   }
   s.swift_version  = '5.9'
   s.source         = { git: 'https://github.com/nickdeupree/expo-alarm-kit' }
diff --git a/ios/ExpoAlarmKitModule.swift b/ios/ExpoAlarmKitModule.swift
index eadb113dd4afecf6ca40e750a385fafb335e3ea5..52d80f34fea3dd3ccc284cf65582c42ec978312b 100644
--- a/ios/ExpoAlarmKitModule.swift
+++ b/ios/ExpoAlarmKitModule.swift
@@ -584,8 +584,18 @@ public class ExpoAlarmKitModule: Module {
             )
             
             // Create alert presentation for when timer fires
+            // PATCH (Vigora): o SDK do iOS 26.1 tornou `stopButton` obrigatório em
+            // AlarmPresentation.Alert — a lib (0.1.11, abr/2026) ainda chamava sem
+            // ele e quebrava a compilação. Reaproveita o mesmo botão "Stop" do
+            // caminho scheduleAlarm/scheduleRepeatingAlarm.
+            let timerStopButton = AlarmButton(
+                text: LocalizedStringResource(stringLiteral: "Stop"),
+                textColor: Color.white,
+                systemImageName: "stop.circle"
+            )
             let alert = AlarmPresentation.Alert(
-                title: LocalizedStringResource(stringLiteral: options.title)
+                title: LocalizedStringResource(stringLiteral: options.title),
+                stopButton: timerStopButton
             )
             
             let presentation = AlarmPresentation(alert: alert, countdown: countdown, paused: paused)
