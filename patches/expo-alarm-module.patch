diff --git a/android/src/main/java/com/expoalarmmodule/Alarm.java b/android/src/main/java/com/expoalarmmodule/Alarm.java
index 49daf38ac5620031fd3d1da8c52c0762d822b96a..72e62129d5639568690d2ffc21ef86bae943a270 100644
--- a/android/src/main/java/com/expoalarmmodule/Alarm.java
+++ b/android/src/main/java/com/expoalarmmodule/Alarm.java
@@ -55,6 +55,27 @@ public class Alarm implements Cloneable {
     String dismissText;
     String snoozeText;
 
+    /**
+     * Tocar o som do alarme? Vem da chave "Som" do formulario do Vigora.
+     *
+     * Boolean (objeto) de proposito, NAO boolean primitivo: os alarmes ja
+     * gravados no Storage foram serializados sem este campo, e o Gson preenche
+     * primitivo ausente com false -- todo alarme existente ficaria MUDO depois
+     * do update. Ausente vira null, e null significa "toca" (ver Manager.start).
+     */
+    Boolean sound;
+
+    /**
+     * Vibrar no disparo? Vem da chave "Vibracao" do formulario do Vigora,
+     * combinada com a global das Configuracoes (Storage.getVibrationEnabled).
+     *
+     * Boolean (objeto) pelo mesmo motivo de `sound`: os alarmes ja gravados no
+     * Storage foram serializados sem este campo, e o Gson preencheria primitivo
+     * ausente com false -- todo alarme existente ficaria sem vibrar. Ausente =
+     * null = vibra (ver Manager.start).
+     */
+    Boolean vibration;
+
     Alarm(String uid, ArrayList<Integer> days, ZonedDateTime date, int hour, int minutes, boolean showDismiss, boolean showSnooze, int snoozeInterval, String title, String description, boolean repeating, boolean active, String dismissText, String snoozeText) {
         this.uid = uid;
         this.days = days;
diff --git a/android/src/main/java/com/expoalarmmodule/AlarmDates.java b/android/src/main/java/com/expoalarmmodule/AlarmDates.java
index 455375ae411fa22e853dbd8c945124690e703e18..fd2862ed2203539357e0943fb13c15554bba7b01 100644
--- a/android/src/main/java/com/expoalarmmodule/AlarmDates.java
+++ b/android/src/main/java/com/expoalarmmodule/AlarmDates.java
@@ -48,6 +48,23 @@ public class AlarmDates {
         return calendar.getTime();
     }
 
+    /**
+     * Avança a data em passos de intervalDays até ficar no FUTURO.
+     * Substitui o setNextWeek fixo no dismiss: alarme diário repete a cada
+     * 1 dia (a lib original assumia sempre semanal — ficava mudo 6 dias) e,
+     * se a data base estiver no passado (alarme atrasado/dispensado tarde),
+     * avança quantos passos forem precisos em vez de agendar no passado
+     * (que dispararia imediatamente).
+     */
+    public static Date setNextOccurrence (Date date, int intervalDays) {
+        Calendar calendar = Calendar.getInstance();
+        calendar.setTime(date);
+        do {
+            calendar.add(Calendar.DATE, intervalDays);
+        } while (calendar.getTimeInMillis() <= System.currentTimeMillis());
+        return calendar.getTime();
+    }
+
     public static Date snooze (Date date, int minutes) {
         Calendar calendar = Calendar.getInstance();
         calendar.setTime(date);
@@ -65,6 +82,13 @@ public class AlarmDates {
     }
 
     public Date getCurrentDate () {
+        // Todos os alarmes do Vigora têm exatamente UMA data por uid (diário,
+        // por-dia-da-semana e soneca usam uids separados). O match por dia da
+        // semana abaixo retornava null para um alarme dispensado depois da
+        // meia-noite (dia da semana já virou) — e o dismiss não reagendava.
+        if (dates.length == 1) {
+            return dates[0];
+        }
         Calendar calendar = Calendar.getInstance();
         int currentDay = calendar.get(Calendar.DAY_OF_WEEK);
         for (Date date : dates) {
diff --git a/android/src/main/java/com/expoalarmmodule/ExpoAlarmModuleModule.java b/android/src/main/java/com/expoalarmmodule/ExpoAlarmModuleModule.java
index 552911d58dbc4123c3eff79031d88bd8e4dc1ed9..e0f0a2d5caddc53ab3b16233c393efebdbb7860f 100644
--- a/android/src/main/java/com/expoalarmmodule/ExpoAlarmModuleModule.java
+++ b/android/src/main/java/com/expoalarmmodule/ExpoAlarmModuleModule.java
@@ -93,6 +93,54 @@ public class ExpoAlarmModuleModule extends ReactContextBaseJavaModule {
     promise.resolve(null);
   }
 
+  /**
+   * Volume do alarme (0-100), vindo do slider das Configurações. Persistido
+   * porque quem toca é o AlarmService, que roda sem o app aberto. Escala só o
+   * MediaPlayer do alarme — o volume de alarme do sistema fica intacto.
+   */
+  @ReactMethod
+  public void setAlarmVolume (int volume, Promise promise) {
+    Storage.saveAlarmVolume(reactContext, volume);
+    promise.resolve(null);
+  }
+
+  /**
+   * Vibracao ligada nas Configuracoes (chave global). Persistida pelo mesmo
+   * motivo do volume: quem vibra no disparo e' o AlarmService, que roda sem o
+   * app aberto -- nao da' para ler a configuracao na hora.
+   */
+  @ReactMethod
+  public void setAlarmVibration (boolean enabled, Promise promise) {
+    Storage.saveVibrationEnabled(reactContext, enabled);
+    promise.resolve(null);
+  }
+
+  /**
+   * Prévia de 1,5s do som do alarme para o teste de volume das Configurações —
+   * o alarme de verdade, não uma imitação em JS. Ver Manager.preview.
+   */
+  @ReactMethod
+  public void previewSound (Promise promise) {
+    Manager.preview(reactContext);
+    promise.resolve(null);
+  }
+
+  /**
+   * Silencia o som do alarme em curso sem desmontá-lo (a tela usa durante a
+   * fala). Não confundir com stop(), que encerra o alarme e reagenda.
+   */
+  @ReactMethod
+  public void pauseSound (Promise promise) {
+    Manager.pauseSound();
+    promise.resolve(null);
+  }
+
+  @ReactMethod
+  public void resumeSound (Promise promise) {
+    Manager.resumeSound();
+    promise.resolve(null);
+  }
+
   @ReactMethod
   public void snooze (Promise promise) {
     Manager.snooze(reactContext);
@@ -176,7 +224,12 @@ public class ExpoAlarmModuleModule extends ReactContextBaseJavaModule {
         }
       }
     }
-    return new Alarm(uid, days, date, hour, minutes, showDismiss, showSnooze, snoozeInterval, title, description, repeating, active, dismissText, snoozeText);
+    Alarm parsed = new Alarm(uid, days, date, hour, minutes, showDismiss, showSnooze, snoozeInterval, title, description, repeating, active, dismissText, snoozeText);
+    // Ausente = toca (compatibilidade com quem agenda sem informar a chave).
+    parsed.sound = alarm.hasKey("sound") ? alarm.getBoolean("sound") : Boolean.TRUE;
+    // Ausente = vibra (compatibilidade com quem agenda sem informar a chave).
+    parsed.vibration = alarm.hasKey("vibration") ? alarm.getBoolean("vibration") : Boolean.TRUE;
+    return parsed;
   }
 
   private WritableMap serializeAlarmObject (Alarm alarm) throws Exception {
diff --git a/android/src/main/java/com/expoalarmmodule/Helper.java b/android/src/main/java/com/expoalarmmodule/Helper.java
index 1c5b665000d21998b8ccfb8ff51e9041cc4c640b..e817495a9846aea2664ed92056cdeff27115ffd5 100644
--- a/android/src/main/java/com/expoalarmmodule/Helper.java
+++ b/android/src/main/java/com/expoalarmmodule/Helper.java
@@ -11,6 +11,7 @@ import android.content.res.Resources;
 import android.graphics.Bitmap;
 import android.graphics.BitmapFactory;
 import android.graphics.Color;
+import android.net.Uri;
 import android.os.Build;
 import android.util.Log;
 
@@ -51,12 +52,29 @@ public class Helper {
             );
         }
 
-        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.M) {
-            alarmManager.setExactAndAllowWhileIdle(AlarmManager.RTC_WAKEUP, triggerAtMillis, pendingIntent);
-        } else if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.KITKAT) {
-            alarmManager.setExact(AlarmManager.RTC_WAKEUP, triggerAtMillis, pendingIntent);
-        } else {
-            alarmManager.set(AlarmManager.RTC_WAKEUP, triggerAtMillis, pendingIntent);
+        // Android 12/12L: SCHEDULE_EXACT_ALARM pode ser revogada pelo usuário
+        // ("Alarmes e lembretes") e USE_EXACT_ALARM só existe no 13+. Sem a
+        // permissão, setExact* lança SecurityException — que aqui derrubaria o
+        // BootReceiver/NotificationActionReceiver. Fallback: alarme inexato
+        // (pode atrasar alguns minutos, mas dispara).
+        try {
+            if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.S && !alarmManager.canScheduleExactAlarms()) {
+                alarmManager.setAndAllowWhileIdle(AlarmManager.RTC_WAKEUP, triggerAtMillis, pendingIntent);
+                Log.w(TAG, "Exact alarms not permitted - scheduled inexact alarm");
+            } else if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.M) {
+                alarmManager.setExactAndAllowWhileIdle(AlarmManager.RTC_WAKEUP, triggerAtMillis, pendingIntent);
+            } else if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.KITKAT) {
+                alarmManager.setExact(AlarmManager.RTC_WAKEUP, triggerAtMillis, pendingIntent);
+            } else {
+                alarmManager.set(AlarmManager.RTC_WAKEUP, triggerAtMillis, pendingIntent);
+            }
+        } catch (SecurityException e) {
+            Log.w(TAG, "Exact alarm rejected, falling back to inexact", e);
+            if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.M) {
+                alarmManager.setAndAllowWhileIdle(AlarmManager.RTC_WAKEUP, triggerAtMillis, pendingIntent);
+            } else {
+                alarmManager.set(AlarmManager.RTC_WAKEUP, triggerAtMillis, pendingIntent);
+            }
         }
         Log.d(TAG, "SDK version: " + Build.VERSION.SDK_INT);
         Log.d(TAG, "scheduling alarm with notification id: " + notificationID);
@@ -100,6 +118,12 @@ public class Helper {
     }
 
     static Notification getAlarmNotification(Context context, Alarm alarm, int notificationID) {
+        if (alarm == null) {
+            // PendingIntent obsoleto (alarme já removido do storage): notificação
+            // genérica em vez de NPE — o NPE derrubava o AlarmService antes do
+            // startForeground (ANR "did not call startForeground" no Android 8+).
+            return getNotification(context, notificationID, "", "Vigora", "Toque para abrir o Vigora", false, false, "", "");
+        }
         return getNotification(context, notificationID, alarm.uid, alarm.title, alarm.description, alarm.showDismiss, alarm.showSnooze, alarm.dismissText, alarm.snoozeText);
     }
 
@@ -122,11 +146,20 @@ public class Helper {
             // Sets the notification light color for notifications posted to this
             // channel, if the device supports this feature.
             channel.setLightColor(Color.RED);
-            channel.enableVibration(true);
-            channel.setVibrationPattern(new long[]{100, 200, 300, 400, 500, 400, 300, 200, 400});
+            // Canal NÃO vibra e NÃO toca: quem faz som é o MediaPlayer do
+            // AlarmService (no STREAM_ALARM) e quem vibra é a tela do alarme,
+            // que respeita as configurações do usuário. Com o canal vibrando
+            // também, desligar a vibração no app não surtia efeito nenhum.
+            channel.enableVibration(false);
+            channel.setSound(null, null);
             // Register the channel with the system; you can't change the importance
             // or other notification behaviors after this
             NotificationManager notificationManager = ContextCompat.getSystemService(context, NotificationManager.class);
+            // As configurações de um canal são IMUTÁVEIS depois de criado: em
+            // quem já tinha o app, o canal antigo continuaria vibrando para
+            // sempre. Por isso o id mudou (ver strings.xml) e o antigo é
+            // removido — senão ele fica órfão nas configurações do Android.
+            notificationManager.deleteNotificationChannel("expo-alarm-module");
             notificationManager.createNotificationChannel(channel);
             Log.d(TAG, "created a notification channel " + channel.toString());
         } else {
@@ -147,7 +180,13 @@ public class Helper {
         ) {
         Resources res = context.getResources();
         String packageName = context.getPackageName();
-        int smallIconResId = res.getIdentifier("ic_launcher", "mipmap", packageName);
+        // Ícone dedicado de notificação (glifo branco em transparência, gerado
+        // pelo plugin expo-notifications) — o ic_launcher adaptativo vira um
+        // quadrado cinza na status bar de muitos aparelhos. Fallback: launcher.
+        int smallIconResId = res.getIdentifier("notification_icon", "drawable", packageName);
+        if (smallIconResId == 0) {
+            smallIconResId = res.getIdentifier("ic_launcher", "mipmap", packageName);
+        }
         String channelId = context.getResources().getString(R.string.notification_channel_id);
     
         PendingIntent pendingIntentDismiss = createActionIntent(context, alarmUid, id, "DISMISS_ACTION");
@@ -162,21 +201,30 @@ public class Helper {
                 .setVisibility(NotificationCompat.VISIBILITY_PUBLIC)
                 .setCategory(NotificationCompat.CATEGORY_ALARM)
                 .setAutoCancel(true)
-                .setOngoing(false)
+                .setOngoing(true)
                 .setSound(null)
                 .setVibrate(null)
-                .setContentIntent(createOnClickedIntent(context, alarmUid, id))
+                .setContentIntent(createOpenIntent(context, alarmUid, id, ""))
+                .setFullScreenIntent(createOpenIntent(context, alarmUid, id, ""), true)
                 .setDeleteIntent(pendingIntentDismiss);
 
-        // If the dismiss button should be visible.
+        // "Dispensar" via deep link (&dismiss=1), NAO via DISMISS_ACTION: o
+        // broadcast nativo so parava o som em Java e o JS nunca sabia -- o evento
+        // do dead man's switch ficava sem confirmacao e o servidor marcava como
+        // "missed" 5 min depois, avisando a familia de um alarme RESPONDIDO.
+        // Mesmo defeito (e mesma correcao) da soneca logo abaixo.
         if(showDismiss) {
-            builder.addAction(android.R.drawable.ic_menu_close_clear_cancel, dismissText, pendingIntentDismiss);
+            builder.addAction(android.R.drawable.ic_menu_close_clear_cancel, dismissText, createOpenIntent(context, alarmUid, id, "&dismiss=1"));
         }
 
         // If snooze is active, add a button for snoozing.
+        // Soneca via deep link (abre o app com &snooze=1), NÃO via SNOOZE_ACTION:
+        // o broadcast nativo reagendava só em Java e o JS nunca sabia — o evento
+        // do dead man's switch ficava sem confirmação e escalava um alarme que o
+        // idoso TINHA atendido. Com o app aberto, alarm-ring confirma "responded",
+        // re-arma +5min e registra o evento backstop no servidor.
         if(showSnooze) {
-            PendingIntent pendingIntentSnoozee = createActionIntent(context, alarmUid, id, "SNOOZE_ACTION");
-            builder.addAction(android.R.drawable.ic_menu_recent_history, snoozeText, pendingIntentSnoozee);
+            builder.addAction(android.R.drawable.ic_menu_recent_history, snoozeText, createOpenIntent(context, alarmUid, id, "&snooze=1"));
         }
 
     
@@ -192,26 +240,28 @@ public class Helper {
         return builder.build();
     }
 
-    private static PendingIntent createOnClickedIntent(Context context, String alarmUid, int notificationID) {
+    /**
+     * PendingIntent que ABRE o app na tela do alarme. `extraQuery` acrescenta a
+     * acao ao deep link ("" = so abrir, "&snooze=1", "&dismiss=1") — quem executa
+     * e confirma no servidor e' sempre o JS (alarm-ring), nunca o Java.
+     *
+     * Deep link explicito (e nao so o ALARM_UID no extra) porque ele roteia de
+     * forma deterministica no cold start do Android antigo, sem depender do
+     * getAlarmState em memoria.
+     *
+     * O requestCode pode repetir entre as acoes: o data URI diferente ja as torna
+     * PendingIntents distintos (Intent.filterEquals).
+     */
+    private static PendingIntent createOpenIntent(Context context, String alarmUid, int notificationID, String extraQuery) {
         Intent resultIntent = new Intent(context, Helper.getMainActivityClass(context));
         resultIntent.putExtra("ALARM_UID", alarmUid);
+        resultIntent.setAction(Intent.ACTION_VIEW);
+        resultIntent.setData(Uri.parse("vigora://alarm-ring?uid=" + Uri.encode(alarmUid) + extraQuery));
 
-        PendingIntent pendingIntent = null;
-        if (android.os.Build.VERSION.SDK_INT >= android.os.Build.VERSION_CODES.S) {  
-            pendingIntent = PendingIntent.getActivity(
-                context,
-                notificationID,
-                resultIntent,
-                PendingIntent.FLAG_MUTABLE);
-        } else {
-            pendingIntent = PendingIntent.getActivity(
-                context,
-                notificationID,
-                resultIntent,
-                PendingIntent.FLAG_UPDATE_CURRENT);
+        if (android.os.Build.VERSION.SDK_INT >= android.os.Build.VERSION_CODES.S) {
+            return PendingIntent.getActivity(context, notificationID, resultIntent, PendingIntent.FLAG_MUTABLE);
         }
-
-        return pendingIntent;
+        return PendingIntent.getActivity(context, notificationID, resultIntent, PendingIntent.FLAG_UPDATE_CURRENT);
     }
 
     /**
diff --git a/android/src/main/java/com/expoalarmmodule/Manager.java b/android/src/main/java/com/expoalarmmodule/Manager.java
index 004075da7464088c26edd8b5e6e2e5a191ec7633..72d36112982269edffa7f8c01a22ebfe8cf9c2a1 100644
--- a/android/src/main/java/com/expoalarmmodule/Manager.java
+++ b/android/src/main/java/com/expoalarmmodule/Manager.java
@@ -8,15 +8,72 @@ import java.util.Objects;
 
 public class Manager {
 
+    /**
+     * Padrao IDENTICO ao da tela (alarm-ring): quando os dois rodam, a segunda
+     * chamada a vibrate() do MESMO app substitui a primeira -- mesmo padrao,
+     * nenhuma diferenca perceptivel. Padroes diferentes fariam a vibracao
+     * "pular" ao abrir a tela.
+     */
+    private static final long[] VIBRATION_PATTERN = new long[]{0, 500, 500, 500};
+    private static android.os.Vibrator vibrator;
+
     private static final String TAG = "AlarmManager";
     private static Sound sound;
     private static String activeAlarmUid;
+    private static Sound previewSound;
+    private static final android.os.Handler previewHandler = new android.os.Handler(android.os.Looper.getMainLooper());
+
+    /**
+     * Toca o som do alarme por ~1,5s para o TESTE de volume das Configurações.
+     * Usa o MESMO Sound do disparo real: mesmo arquivo, mesmo STREAM_ALARM,
+     * mesma curva de volume. O teste antigo tocava outro arquivo (assets/
+     * alarm.mp3) por outro pipeline (expo-audio/ExoPlayer) — soava diferente do
+     * alarme de verdade e não havia como o usuário confiar nele.
+     */
+    public static void preview(Context context) {
+        // Cancela o postDelayed do preview anterior: sem isso, trocar de opção
+        // rápido empilha vários "pare em 1,5s" e um deles acaba disparando no
+        // MEIO de um preview mais novo, cortando o final — e como o callback
+        // velho não é cancelado, ele continua "roubando" o próximo preview até
+        // esvaziar a fila (som cortado ficava parecendo permanente).
+        previewHandler.removeCallbacksAndMessages(null);
+        stopPreview();
+        previewSound = new Sound(context);
+        previewSound.play("default");
+        previewHandler.postDelayed(Manager::stopPreview, 1500);
+    }
+
+    private static void stopPreview() {
+        if (previewSound != null) {
+            previewSound.stop();
+            previewSound = null;
+        }
+    }
 
     static String getActiveAlarm() {
         return activeAlarmUid;
     }
 
+    /**
+     * Cancela os PendingIntents dos disparos ANTERIORES deste uid. Obrigatório
+     * antes de qualquer (re)agendamento: cada AlarmDates nasce com
+     * notificationIds ALEATÓRIOS novos, e o app reagenda todos os alarmes a
+     * cada abertura (syncAlarmsOnStartup). Sem cancelar, os PendingIntents
+     * antigos ficavam armados no AlarmManager — "alarmes fantasma" que
+     * disparavam em rajada no horário: som reiniciando, full-screen relançada
+     * (voz repetindo em fila), instâncias empilhadas escalando sozinhas, e
+     * notificação genérica para alarme já desativado/removido do storage.
+     */
+    private static void cancelPreviousDates(Context context, String alarmUid) {
+        AlarmDates previous = Storage.getDates(context, alarmUid);
+        if (previous == null) return;
+        for (Date date : previous.getDates()) {
+            Helper.cancelAlarm(context, previous.getNotificationId(date));
+        }
+    }
+
     static void schedule(Context context, Alarm alarm) {
+        cancelPreviousDates(context, alarm.uid);
         AlarmDates dates = alarm.getAlarmDates();
         for (Date date : dates.getDates()) {
             Helper.scheduleAlarm(context, alarm.uid, date.getTime(), dates.getNotificationId(date));
@@ -28,6 +85,7 @@ public class Manager {
     public static void reschedule(Context context) {
         Alarm[] alarms = Storage.getAllAlarms(context);
         for (Alarm alarm : alarms) {
+            cancelPreviousDates(context, alarm.uid);
             Storage.removeDates(context, alarm.uid);
             AlarmDates dates = alarm.getAlarmDates();
             Storage.saveDates(context, dates);
@@ -60,6 +118,7 @@ public class Manager {
     }
 
     static void remove(Context context, String alarmUid) {
+        stopVibration();
         if (sound != null) {
             sound.stop();
         }
@@ -112,16 +171,103 @@ public class Manager {
     }
 
     static void start(Context context, String alarmUid) {
+        // Idempotente. O AlarmService é START_STICKY e onStartCommand roda mais
+        // de uma vez por disparo; a versão original criava um Sound novo a cada
+        // chamada e ORFANAVA o MediaPlayer anterior — que tocava em loop
+        // (setLooping(true)) sem nenhuma referência capaz de pará-lo. Resultado:
+        // o som continuava depois de dispensar o alarme, até o processo morrer
+        // ("MediaPlayer finalized without being released" no logcat).
+        if (sound != null) {
+            sound.stop();
+        }
         activeAlarmUid = alarmUid;
+
+        // "Som" desmarcado no formulario: o alarme continua valendo (tela cheia,
+        // notificacao, vibracao, voz e o dead man's switch), so nao toca. Antes
+        // o servico tocava sempre e quem calava era a tela do alarme ao iniciar
+        // a fala -- por isso o som saia por 1-2s mesmo com a opcao desmarcada.
+        // sound fica null, entao pauseSound/resumeSound viram no-op sozinhos.
+        Alarm alarm = Storage.getAlarm(context, alarmUid);
+        boolean soundEnabled = alarm == null || alarm.sound == null || alarm.sound;
+        if (!soundEnabled) {
+            // Zera a referencia: o stop() acima parou o som do disparo anterior
+            // mas nao soltou o objeto, e um resumeSound() (fim da fala do TTS)
+            // ressuscitaria o alarme ERRADO no meio de um alarme silencioso.
+            sound = null;
+            Log.d(TAG, "Alarme " + alarmUid + " configurado sem som - nao vai tocar");
+            startVibration(context, alarm);
+            return;
+        }
+
         sound = new Sound(context);
         sound.play("default");
+        // Depois do sound.play: o sound.stop() do inicio deste metodo cancela a
+        // vibracao do app inteiro (Vibrator.cancel e' por app), e comecar antes
+        // seria comecar para ser cancelado.
+        startVibration(context, alarm);
 
         Log.d(TAG, "Starting " + activeAlarmUid);
     }
 
+    /**
+     * Vibra enquanto o alarme toca. Fica AQUI, e nao na tela, porque a tela nem
+     * sempre abre: sem a permissao de full-screen intent (ou com o aparelho em
+     * uso), o disparo vira so' uma notificacao heads-up e o alarme ficava mudo
+     * de vibracao -- justamente onde o idoso mais depende dela.
+     *
+     * Nao vibra sempre, que era o motivo de ter sido removido daqui: le as DUAS
+     * chaves do app -- a global (persistida por setAlarmVibration) e a do proprio
+     * alarme (campo `vibration`, gravado no agendamento). A tela continua
+     * vibrando com as chaves lidas ao vivo; quando ambos rodam, a chamada da tela
+     * apenas substitui esta (mesmo padrao).
+     *
+     * Independente do som: "Som" desmarcado no formulario nao desliga a vibracao.
+     */
+    private static void startVibration(Context context, Alarm alarm) {
+        boolean alarmWants = alarm == null || alarm.vibration == null || alarm.vibration;
+        if (!alarmWants || !Storage.getVibrationEnabled(context)) {
+            Log.d(TAG, "Alarme " + activeAlarmUid + " sem vibracao (alarme=" + alarmWants
+                    + ", global=" + Storage.getVibrationEnabled(context) + ")");
+            return;
+        }
+        vibrator = (android.os.Vibrator) context.getSystemService(Context.VIBRATOR_SERVICE);
+        if (vibrator == null || !vibrator.hasVibrator()) {
+            Log.w(TAG, "Aparelho sem vibrador - alarme so' com som");
+            return;
+        }
+        // repeat=0: repete o padrao do indice 0 ate cancel(). Todo caminho que
+        // encerra o disparo passa por stopVibration() -- sem isso vibra sempre.
+        vibrator.vibrate(VIBRATION_PATTERN, 0);
+    }
+
+    private static void stopVibration() {
+        if (vibrator != null) {
+            vibrator.cancel();
+            vibrator = null;
+        }
+    }
+
+    /**
+     * Silencia/retoma o som do alarme em curso, sem desmontá-lo. A tela do
+     * alarme usa isto durante a fala (TTS) — `stop()` não serve porque também
+     * reagenda a recorrência e zera o activeAlarmUid.
+     */
+    public static void pauseSound() {
+        if (sound != null) {
+            sound.pause();
+        }
+    }
+
+    public static void resumeSound() {
+        if (sound != null) {
+            sound.resume();
+        }
+    }
+
     public static void stop(Context context) {
         Log.d(TAG, "Stopping " + activeAlarmUid);
 
+        stopVibration();
         if(Objects.nonNull(sound)) {
             sound.stop();
         }
@@ -131,11 +277,21 @@ public class Manager {
             AlarmDates dates = Storage.getDates(context, activeAlarmUid);
             if(alarm != null) {
                 if (alarm.repeating) {
-                    Date current = dates.getCurrentDate();
-                    Date updated = AlarmDates.setNextWeek(current);
+                    // dates/current podem ser null (storage inconsistente após
+                    // limpeza/reinstalação) — NPE aqui crasharia o receiver do
+                    // botão "Dispensar". Sem eles, o app reagenda na abertura.
+                    Date current = dates != null ? dates.getCurrentDate() : null;
+                    if (current != null) {
+                    // BUG CRÍTICO corrigido: a lib original sempre reagendava
+                    // +7 dias (setNextWeek) — alarme DIÁRIO do Vigora (uid sem
+                    // sufixo "_wd") ficava mudo por 6 dias após ser dispensado.
+                    // Diário = +1 dia; por-dia-da-semana (uid "..._wd<N>") = +7.
+                    int intervalDays = alarm.uid.contains("_wd") ? 7 : 1;
+                    Date updated = AlarmDates.setNextOccurrence(current, intervalDays);
                     dates.update(current, updated);
                     Storage.saveDates(context, dates);
                     Helper.scheduleAlarm(context, dates.alarmUid, updated.getTime(), dates.getCurrentNotificationId());
+                    }
                 } else {
                     alarm.active = false;
                     Storage.saveAlarm(context, alarm);
@@ -149,6 +305,7 @@ public class Manager {
     public static void snooze(Context context) {
         Log.d(TAG, "Snoozing " + activeAlarmUid);
 
+        stopVibration();
         if(Objects.nonNull(sound)) {
             sound.stop();
         }
diff --git a/android/src/main/java/com/expoalarmmodule/Sound.java b/android/src/main/java/com/expoalarmmodule/Sound.java
index 73b00616707cd5533258ce215fb1fb1e2deafb50..9b42dd7019fe96f019ec3d6a6cc77674d5573cda 100644
--- a/android/src/main/java/com/expoalarmmodule/Sound.java
+++ b/android/src/main/java/com/expoalarmmodule/Sound.java
@@ -4,8 +4,6 @@ import android.content.Context;
 import android.media.AudioManager;
 import android.media.MediaPlayer;
 import android.net.Uri;
-import android.os.Build;
-import android.os.VibrationEffect;
 import android.os.Vibrator;
 import android.provider.Settings;
 import android.util.Log;
@@ -13,39 +11,74 @@ import android.util.Log;
 class Sound {
 
     private static final String TAG = "AlarmSound";
-    private static final long DEFAULT_VIBRATION = 100;
 
-    private AudioManager audioManager;
-    private int userVolume;
     private MediaPlayer mediaPlayer;
     private Vibrator vibrator;
+    /** Após stop() o MediaPlayer está liberado — pause/resume viram no-op. */
+    private boolean released = false;
 
     private Context context;
 
     Sound(Context context) {
         this.context = context;
         this.vibrator = (Vibrator) context.getSystemService(Context.VIBRATOR_SERVICE);
-        this.audioManager = (AudioManager) context.getSystemService(Context.AUDIO_SERVICE);
-        this.userVolume = audioManager.getStreamVolume(AudioManager.STREAM_ALARM);
         this.mediaPlayer = new MediaPlayer();
     }
 
     void play(String sound) {
         Uri soundUri = getSoundUri(sound);
         playSound(soundUri);
-        startVibration();
+        // Vibração NÃO sai daqui: este serviço não conhece as configurações do
+        // usuário (a global e a do alarme), então vibrava sempre — desligar no
+        // app não tinha efeito. Quem vibra é a tela do alarme, que lê as duas
+        // chaves ao vivo. Ver docs/claude/alarmes.md.
     }
 
     void stop() {
+        released = true;
         try {
             if (mediaPlayer.isPlaying()) {
                 stopSound();
-                stopVibration();
-                mediaPlayer.release();
             }
         } catch (IllegalStateException e) {
             Log.d(TAG, "Sound has probably been released already");
         }
+        try {
+            mediaPlayer.release();
+        } catch (Exception e) {
+            Log.d(TAG, "MediaPlayer already released");
+        }
+        // Sempre cancela a vibração: se o som falhou ao iniciar (isPlaying()
+        // false), a versão original nunca chamava stopVibration() e o padrão
+        // repetido (repeat=0) vibrava para sempre.
+        stopVibration();
+    }
+
+    /**
+     * Silencia o som SEM desmontar o alarme — a tela usa isto enquanto a voz
+     * (TTS) fala, para a fala ser ouvida. `stop()` não serve: ele libera o
+     * player e ainda reagenda a recorrência lá no Manager.
+     */
+    void pause() {
+        if (released) return;
+        try {
+            if (mediaPlayer.isPlaying()) {
+                mediaPlayer.pause();
+            }
+        } catch (IllegalStateException e) {
+            Log.d(TAG, "Cannot pause - player already released");
+        }
+    }
+
+    void resume() {
+        if (released) return;
+        try {
+            if (!mediaPlayer.isPlaying()) {
+                mediaPlayer.start();
+            }
+        } catch (IllegalStateException e) {
+            Log.d(TAG, "Cannot resume - player already released");
+        }
     }
 
     private void playSound(Uri soundUri) {
@@ -54,7 +87,17 @@ class Sound {
                 mediaPlayer.setScreenOnWhilePlaying(true);
                 mediaPlayer.setAudioStreamType(AudioManager.STREAM_ALARM);
                 mediaPlayer.setDataSource(context, soundUri);
-                mediaPlayer.setVolume(100, 100);
+                // Volume do slider do app (0-100). setVolume recebe um escalar
+                // 0..1 aplicado SÓ a este player — não mexe no volume de alarme
+                // do sistema. O original passava (100,100), que satura em 1.0:
+                // o slider do app não governava nada.
+                // Curva QUADRÁTICA: o escalar é linear em amplitude e o ouvido
+                // é logarítmico — linear fazia 10% soar igual a 100%. Com x²,
+                // 50% ≈ -12dB e 10% ≈ -40dB: degraus audíveis. Espelhada no
+                // preview das Configurações (settings.tsx) — mudou aqui, mude lá.
+                float linear = Storage.getAlarmVolume(context) / 100f;
+                float volume = linear * linear;
+                mediaPlayer.setVolume(volume, volume);
                 mediaPlayer.setLooping(true);
                 mediaPlayer.prepare();
                 mediaPlayer.start();
@@ -66,8 +109,10 @@ class Sound {
 
     private void stopSound() {
         try {
-            // reset the volume to what it was before we changed it.
-            audioManager.setStreamVolume(AudioManager.STREAM_ALARM, userVolume, AudioManager.FLAG_PLAY_SOUND);
+            // O original "restaurava" o volume do stream de alarme aqui, resquício
+            // de quando ele subia esse volume. Hoje quem escala é setVolume() no
+            // próprio player: o stream do sistema nunca é tocado, e a restauração
+            // só servia para emitir o bipe do FLAG_PLAY_SOUND a cada parada.
             mediaPlayer.stop();
             mediaPlayer.reset();
         } catch (Exception e) {
@@ -76,23 +121,6 @@ class Sound {
         }
     }
 
-    private void startVibration() {
-        vibrator.vibrate(DEFAULT_VIBRATION);
-        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
-            vibrator.vibrate(VibrationEffect.createOneShot(5000, VibrationEffect.DEFAULT_AMPLITUDE));
-        } else {
-            //deprecated in API 26
-            vibrator.vibrate(500);
-        }
-
-        long[] pattern = {0, 100, 1000};
-
-        // The '0' here means to repeat indefinitely
-        // '0' is actually the index at which the pattern keeps repeating from (the start)
-        // To repeat the pattern from any other point, you could increase the index, e.g. '1'
-        vibrator.vibrate(pattern, 0);
-    }
-
     private void stopVibration() {
         vibrator.cancel();
     }
@@ -100,7 +128,16 @@ class Sound {
     private Uri getSoundUri(String soundName) {
         Uri soundUri;
         if (soundName.equals("default")) {
-            soundUri = Settings.System.DEFAULT_RINGTONE_URI;
+            // Som de alarme do app (res/raw/alarm_notification.wav, empacotado
+            // pelo plugin expo-notifications) — o mesmo som em qualquer aparelho.
+            // O DEFAULT_RINGTONE_URI original varia por dispositivo e fica MUDO
+            // quando o toque do celular é "Nenhum" (comum em aparelhos antigos).
+            int rawId = context.getResources().getIdentifier("alarm_notification", "raw", context.getPackageName());
+            if (rawId != 0) {
+                return Uri.parse("android.resource://" + context.getPackageName() + "/" + rawId);
+            }
+            Uri alarmUri = android.media.RingtoneManager.getDefaultUri(android.media.RingtoneManager.TYPE_ALARM);
+            soundUri = alarmUri != null ? alarmUri : Settings.System.DEFAULT_RINGTONE_URI;
         } else {
             int resId;
             if (context.getResources().getIdentifier(soundName, "raw", context.getPackageName()) != 0) {
diff --git a/android/src/main/java/com/expoalarmmodule/Storage.java b/android/src/main/java/com/expoalarmmodule/Storage.java
index acb1e33bce9bb27c708ef2b0592db2ccc2c70892..14d68e96bf6a02c95edf4ca047164465e0dfbc54 100644
--- a/android/src/main/java/com/expoalarmmodule/Storage.java
+++ b/android/src/main/java/com/expoalarmmodule/Storage.java
@@ -4,7 +4,6 @@ import android.content.Context;
 import android.content.SharedPreferences;
 import android.os.Build;
 
-import com.expoalarmmodule.R;
 import com.google.gson.Gson;
 
 import java.util.ArrayList;
@@ -78,9 +77,41 @@ class Storage {
         return sharedPreferences.edit();
     }
 
+    /** Volume do alarme (0-100), escrito pelo app via ExpoAlarmModule.setAlarmVolume. */
+    private static final String VOLUME_KEY = "vigora_alarm_volume";
+
+    static void saveAlarmVolume(Context context, int volume) {
+        SharedPreferences.Editor editor = getEditor(context);
+        editor.putInt(VOLUME_KEY, Math.max(0, Math.min(100, volume)));
+        editor.apply();
+    }
+
+    /** 100 (máximo) quando o app ainda não escreveu — o comportamento de sempre. */
+    static int getAlarmVolume(Context context) {
+        return getSharedPreferences(context).getInt(VOLUME_KEY, 100);
+    }
+
+    /** Vibracao ligada nas Configuracoes, escrita via ExpoAlarmModule.setAlarmVibration. */
+    private static final String VIBRATION_KEY = "vigora_alarm_vibration";
+
+    static void saveVibrationEnabled(Context context, boolean enabled) {
+        SharedPreferences.Editor editor = getEditor(context);
+        editor.putBoolean(VIBRATION_KEY, enabled);
+        editor.apply();
+    }
+
+    /** true quando o app ainda nao escreveu -- nao vibrar por falta de informacao e' pior. */
+    static boolean getVibrationEnabled(Context context) {
+        return getSharedPreferences(context).getBoolean(VIBRATION_KEY, true);
+    }
+
     private static SharedPreferences getSharedPreferences(Context context) {
-        String fileKey = context.getResources().getString(R.string.notification_channel_id);
-        return context.getSharedPreferences(fileKey, Context.MODE_PRIVATE);
+        // Nome FIXO, desacoplado do notification_channel_id. Eram a mesma
+        // string, então mudar o id do canal (que precisou mudar, porque as
+        // configurações de um NotificationChannel são imutáveis) trocaria
+        // também o arquivo de preferências — e todo alarme já agendado sumiria
+        // da vista do módulo. Este é o nome histórico: preserva os dados.
+        return context.getSharedPreferences("expo-alarm-module", Context.MODE_PRIVATE);
     }
 
 }
diff --git a/android/src/main/res/values/strings.xml b/android/src/main/res/values/strings.xml
index aa5b034b1438475db3b484b82b7e645f7ff5e9c0..a3ce3a8cc41e7157cfc3348e81c6eb49bc5c0aa9 100644
--- a/android/src/main/res/values/strings.xml
+++ b/android/src/main/res/values/strings.xml
@@ -1,6 +1,10 @@
 <?xml version="1.0" encoding="utf-8"?>
 <resources>
-  <string name="notification_channel_id">expo-alarm-module</string>
-  <string name="notification_channel_name">expo-alarm-channel</string>
-  <string name="notification_channel_desc">Ativa o alarm</string>
+  <!-- Id NOVO de propósito: as configurações de um NotificationChannel são
+       imutáveis depois de criado, e o canal antigo ("expo-alarm-module") ficou
+       gravado com vibração ligada em todo aparelho que já teve o app. Nome e
+       descrição são visíveis nas configurações do Android — daí o PT-BR. -->
+  <string name="notification_channel_id">vigora-alarme-v2</string>
+  <string name="notification_channel_name">Alarmes de medicamento</string>
+  <string name="notification_channel_desc">Avisos dos seus alarmes de medicamento</string>
 </resources>
diff --git a/lib/commonjs/models/Alarm.js b/lib/commonjs/models/Alarm.js
index 125b8e88fe8e1b460734d1add901e0aa3aa80881..0d4e01938486186a52c9e5204ae54c92365221f5 100644
--- a/lib/commonjs/models/Alarm.js
+++ b/lib/commonjs/models/Alarm.js
@@ -19,6 +19,8 @@ class Alarm {
     this.snoozeText = (0, _utils.getParam)(params, 'snoozeText');
     this.repeating = (0, _utils.getParam)(params, 'repeating');
     this.active = (0, _utils.getParam)(params, 'active');
+    this.sound = (0, _utils.getParam)(params, 'sound');
+    this.vibration = (0, _utils.getParam)(params, 'vibration');
     this.day = (0, _utils.getParam)(params, 'day');
   }
   static getEmpty() {
diff --git a/lib/module/models/Alarm.js b/lib/module/models/Alarm.js
index 5378c0ab6d1e1ff1335e3740d13243549a07b77a..8a8e8e53fa7c43dc702787751f48f55aac6aa4ba 100644
--- a/lib/module/models/Alarm.js
+++ b/lib/module/models/Alarm.js
@@ -13,6 +13,8 @@ class Alarm {
     this.snoozeText = getParam(params, 'snoozeText');
     this.repeating = getParam(params, 'repeating');
     this.active = getParam(params, 'active');
+    this.sound = getParam(params, 'sound');
+    this.vibration = getParam(params, 'vibration');
     this.day = getParam(params, 'day');
   }
   static getEmpty() {
diff --git a/src/models/Alarm.tsx b/src/models/Alarm.tsx
index c0bd28157e94e70e7c701f19718d90801069f913..2e02f4d6e22dc2c7552a03422f2751bb8d0370d0 100644
--- a/src/models/Alarm.tsx
+++ b/src/models/Alarm.tsx
@@ -13,6 +13,10 @@ class Alarm {
   snoozeText?: string | undefined;
   repeating?: boolean | undefined;
   active?: boolean | undefined;
+  // Vigora: sem esta linha o campo e' descartado pelo construtor abaixo e
+  // 'sound' nunca chega ao nativo -- todo alarme tocava.
+  sound?: boolean | undefined;
+  vibration?: boolean | undefined;
   day?: string | Date | number[] | undefined;
 
   constructor(params: any = null) {
@@ -28,6 +32,8 @@ class Alarm {
     this.snoozeText = getParam(params, 'snoozeText');
     this.repeating = getParam(params, 'repeating');
     this.active = getParam(params, 'active');
+    this.sound = getParam(params, 'sound');
+    this.vibration = getParam(params, 'vibration');
     this.day = getParam(params, 'day');
   }
 
