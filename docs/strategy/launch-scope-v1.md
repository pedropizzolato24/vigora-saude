# Launch scope — v1

## In scope (launch blockers)

- **Alarm reliability** — fix bug causing alarms to not fire sometimes. Core safety feature.
- **Monitored–caregiver link** — logic is built but using placeholder values; must be functional at launch. Dead man's switch alerts depend on it.
- **Daily standalone check-in** — "tudo bem?" prompt at a user-set time, auto-escalates to contacts if no response. Adds today. Closes the coverage gap in the core differentiator.
- **UI accessibility overhaul** — centralized theme update (colors, font sizes, contrast), simplified language throughout, 5–6 form screens converted to wizard step-by-step flows.
- **Caregiver workflow** — screens and functionality built; needs polish and testing before launch.

## Out of scope (post-launch)

- Animations and transitions
- Codebase refactor
- Unknown bugs surfaced after launch (managed through Phase 04 user testing)
